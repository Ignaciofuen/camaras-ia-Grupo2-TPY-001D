import EmptyState from '../components/shared/EmptyState';

/**
 * UserManager
 * Módulo para la administración de cuentas, roles y permisos (RBAC).
 */
const UserManager = () => {
  return (
    <div className="flex flex-col w-full h-full bg-[#0a0a0a]">
      <EmptyState text="MÓDULO DE GESTIÓN DE USUARIOS EN DESARROLLO" />
    </div>
  );
};

export default UserManager;
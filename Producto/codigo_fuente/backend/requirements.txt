# Camaras-IA · Backend FastAPI (sin YOLO/OpenCV — eso vive en el detector)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# DB
psycopg[binary,pool]==3.2.3

# Cache + pubsub
redis==5.0.8

# Object storage (MinIO compatible S3)
boto3==1.35.36

# Auth
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# Env / utilidades
python-dotenv==1.0.1
